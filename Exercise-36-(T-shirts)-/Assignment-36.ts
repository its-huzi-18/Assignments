// Exercise-36 (T-Shirts)

function make_shirt(size: string, message: string): void {
    console.log(`A ${size}-sized shirt will be printed with the message: "${message}".`);
}

// Call the function
make_shirt("Medium", "I love TypeScript!");
